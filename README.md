# Zacharybutler Tap

## How do I install these formulae?

`brew install zacharybutler/tap/<formula>`

Or `brew tap zacharybutler/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "zacharybutler/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
